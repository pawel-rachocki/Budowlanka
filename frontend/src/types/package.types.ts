export interface ListingPackage {
  id: string
  name: string
  durationDays: number
  pricePln: number
}

export interface BoostPackage {
  id: string
  name: string
  durationDays: number
  pricePln: number
}
