# Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/budowlanka
spring.datasource.username=budowlanka
spring.datasource.password=budowlanka

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Mail (Mailtrap)
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=TWOJ_MAILTRAP_USER
spring.mail.password=TWOJE_MAILTRAP_PASS
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
app.jwt.secret=ZMIEN_NA_MIN_32_ZNAKOWY_SECRET_KLUCZ_W_PRODUKCJI
app.jwt.access-token-expiration=900000
app.jwt.refresh-token-expiration=604800000

# App
app.base-url=http://localhost:8080